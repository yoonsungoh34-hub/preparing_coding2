// c++ 문자열 처리

// 숫자를 문자열로 변환 (예: 13 -> "13")
string str_i = to_string(i);

// 문자열에 '3', '6', '9'가 하나라도 포함되어 있는지 찾습니다.
// string::npos는 "찾지 못했다"라는 뜻입니다. 즉, npos가 아니면 찾았다는 의미!
if (str_i.find('3') != string::npos)
